# DACON 자동차 뉴스 분류 프로젝트 의존성

# 핵심 라이브러리
openai==1.10.0      # OpenAI API 클라이언트
pandas==2.0.3       # 데이터 처리
numpy==1.24.3       # 수치 연산

# 선택적 라이브러리 (분석 및 시각화용)
matplotlib==3.7.2   # 시각화
seaborn==0.12.2     # 고급 시각화
jupyter==1.0.0      # 노트북 환경 (선택사항)

# 유틸리티
python-dotenv==1.0.0  # 환경변수 관리 (선택사항)
tqdm==4.65.0          # 진행 표시줄 (선택사항)